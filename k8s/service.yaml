apiVersion: v1
kind: Service
metadata:
  name: telecom-frontend
spec:
  type: NodePort
  selector:
    app: telecom-frontend
  ports:
    - port: 3000
      targetPort: 80
      protocol: TCP
      nodePort: 30007 
