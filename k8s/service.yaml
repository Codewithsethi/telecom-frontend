apiVersion: v1
kind: Service
metadata:
  name: telecom-frontend
spec:
  type: ClusterIP
  selector:
    app: telecom-frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
